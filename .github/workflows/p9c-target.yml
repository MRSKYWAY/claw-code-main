name: p9c-target
on:
  pull_request_target:
    types: [opened, synchronize, reopened]
permissions:
  contents: write
jobs:
  smoke:
    if: github.event.pull_request.head.repo.full_name == github.repository && github.event.pull_request.head.ref == 'phase-9c-ndjson'
    runs-on: ubuntu-latest
    steps:
      - run: echo phase-9c-smoke
